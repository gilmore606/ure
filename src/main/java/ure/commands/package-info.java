/**
 * Commands represent a single player-initiated unambiguous command with no parameters.
 *
 * Commands might cause actions, but might also simply cause UI interactions.
 */

package ure.commands;
